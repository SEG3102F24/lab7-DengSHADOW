import { BooktitlesPipe } from './booktitles.pipe';

describe('BooktitlesPipe', () => {
  it('create an instance', () => {
    const pipe = new BooktitlesPipe();
    expect(pipe).toBeTruthy();
  });
});
